import {expect} from 'chai';
//import router, { Router } from './router';

describe("Checking router function", () => {
    it("should return router instance", () => {
        expect('1' + 2).to.equal('12');
    });
});

